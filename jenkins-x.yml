buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - sh: python -m pip install pytest
        replace: true
        steps:
        - sh: pytest
    release:
      build:
        preSteps:
        - sh: python -m pip install pytest
        replace: true
        steps:
        - sh: pytest

