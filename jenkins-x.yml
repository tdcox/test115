buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        steps:
        - sh: python -m pip install pytest hypothesis && pytest
    release:
      build:
        steps:
        - sh: python -m pip install pytest hypothesis && pytest

