buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - sh: python -m pip install pytest && pytest
    release:
      build:
        replace: true
        steps:
        - sh: python -m pip install pytest && pytest

